import { connect } from 'react-redux';
import EditDocumentDisplay from './display';

function mapStateToProps() {
    return {

    }
}

function mapDispatchToProps(dispatch) {
    return {

    }
}

export default connect(mapStateToProps, mapDispatchToProps)(EditDocumentDisplay);